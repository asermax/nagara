We ship <software> to users and set <your-api-key> in the config.

<software>

The <div> element and its </div> closer, a generic <T> parameter, and a <br/> void tag.

Angle math: 3 < 4 and 5 > 2, plus <3 hearts and a<b compare.

Mail <a@b.com> or read <https://x.test> now.

A run-in **bold**<software>, a `code`<software>, and a [link](https://x.test)<software>.

Use `<software>` inside a code span.

```html
<div>raw markup stays</div>
```

| Name | Tag |
| --- | --- |
| widget | <software> |
