[project]
name = "exp-002-markdown-paragraphs"
version = "0"
description = "Spike: does carrying trafilatura markdown break TTS/timing? (experiment 002)"
requires-python = "==3.12.*"
dependencies = [
    "trafilatura",
    "markdown-it-py",
    "modal",
]

[tool.uv]
package = false
